/* --- Server --- */

export const DEV_SERVER = "http://localhost:4000";

/* --- Requests --- */

export const REQ = {
  LOGIN: DEV_SERVER + "/login",
  SIGNUP: DEV_SERVER + "/signup",
  GET_USER: DEV_SERVER + "/getUser",
  PRODUCTS: {
    GET_PRODUCTS: DEV_SERVER + "/products",
  },
};
